metadata:
  name: "Check that all resources are tagged with the key - Application for AppDNA"
  id: "CKV_AWS_999999"
  category: "GENERAL_SECURITY"
scope:
  provider: aws
definition:
  cond_type: "attribute"
  resource_types: 
    - "aws_s3_bucket"
    - "aws_db_instance"
    - "aws_instance"
  attribute: "tags.Application"
  operator: "exists"